<?xml version="1.0" encoding="utf-8"?>
<metadata>
    <layout title="Single Package">
        <help
                key = "Single Package"
                />
        <message>
            <![CDATA[Select a Package]]>
        </message>
    </layout>

    <!-- Add fields to the request variables for the layout. -->
    <fields name="request">
        <fieldset name="request"
                  addfieldpath="/administrator/components/com_downloadmanager/models/fields">

            <field name="id" type="modal_package"
                   label="Select a Package"
                   required="true"
                   edit="false"
                   clear="false"
                   description="Select a Package"
                    />
        </fieldset>
    </fields>

    <!-- Add fields to the parameters object for the layout. -->
    <fields name="params">

        <!-- Basic options. -->
        <fieldset name="basic"
                  label="Package Info">

            <field
                    name="show_title"
                    type="radio"
                    class="btn-group"
                    label="JGLOBAL_SHOW_TITLE_LABEL"
                    description="JGLOBAL_SHOW_TITLE_DESC">
                <option value="">JGLOBAL_USE_GLOBAL</option>
                <option value="1">JSHOW</option>
                <option value="0">JHIDE</option>
            </field>


        </fieldset>
    </fields>
</metadata>
